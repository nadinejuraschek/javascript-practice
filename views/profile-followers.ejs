<%- include('includes/header') %>

<%- include("includes/sidebar") -%>

<main>

<div class="feed min-height container">

  <!-- FLASH MESSAGE -->
  <%- include("includes/flash") -%>

  <!-- PROFILE -->
  <%- include("includes/profileShared") %>

  <div class="list-group">
    <% followers.forEach(function(follower) { %>
      <div class="list-group-item list-group-item-author">
        <div class="list-groupt-item-author">
          <img class="avatar-tiny" src="<%= follower.avatar %>">
          <a href="/profile/<%= follower.username %>"><strong><%= follower.username %></strong></a>
        </div>
      </div>
    <% }) %>
  </div>
</div>

</main>

<%- include('includes/footer') %>