<%- include('includes/header') %>

<%- include("includes/sidebar") -%>

<main>

<div class="feed min-height container">

  <!-- FLASH MESSAGE -->
  <%- include("includes/flash") -%>

  <!-- PROFILE -->
  <%- include("includes/profileShared") %>

  <div class="list-group">
    <% posts.forEach(function(post) { %>
      <div class="list-group-item list-group-item-action">
        <!-- <div class="list-groupt-item-author">
          <img class="avatar-tiny" src="<%= post.author.avatar %>">
          <p><strong><%= post.author.username %></strong></p>
        </div> -->
        <div class="list-group-item-content">
          <a href="/post/<%= post._id %>"><strong><%= post.title %></strong> on <%= post.createdDate.getMonth() + 1 %>/<%= post.createdDate.getDate() %>/<%= post.createdDate.getFullYear() %></a>
        </div>
      </div>
    <% }) %>
  </div>
</div>

</main>

<%- include('includes/footer') %>